import React from 'react';
import { Button, Popup } from 'semantic-ui-react';

const PopupEEA = () => (
  <Popup content="Add users to your feed" trigger={<Button icon="add" />} />
);

export default PopupEEA;
